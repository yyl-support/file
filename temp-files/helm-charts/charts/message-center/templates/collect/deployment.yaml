{{- if .Values.collectDeployment.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.collectDeployment.name }}
  namespace: {{ .Values.namespace }}
  {{- with .Values.deploymentLabels }}
  labels:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.collectDeployment.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.collectDeployment.name }}
  strategy:
    {{- toYaml .Values.collectDeployment.strategy | nindent 4 }}
  template:
    metadata:
      annotations:
        {{- toYaml .Values.collectDeployment.podAnnotations | nindent 8 }}
      labels:
        app: {{ .Values.collectDeployment.name }}
    spec:
      serviceAccount: {{ .Values.serviceAccount.name }}
      imagePullSecrets:
        - name: {{ .Values.imagePullSecrets.name }}
      automountServiceAccountToken: false
      containers:
        - name: server
          args:
            {{- toYaml .Values.collectDeployment.args | nindent 12 }}
          image: "{{ .Values.collectDeployment.image.repository }}:{{ .Values.collectDeployment.image.tag }}"
          imagePullPolicy: {{ .Values.collectDeployment.image.pullPolicy }}
          resources:
            {{- toYaml .Values.collectDeployment.resources | nindent 12 }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
      volumes:
        {{- toYaml .Values.volumes | nindent 8 }}
{{- end }}
