{{- if .Values.errata.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.errata.name }}-service
  labels:
    {{- with .Values.errata.podLabels }}
    {{- toYaml . | nindent 4}}
    {{- end }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.errata.service.port }}
      targetPort: {{ .Values.errata.service.targetPort }}
      protocol: TCP
      name: http-port
  {{- with .Values.errata.podLabels }}
  selector:
    {{- toYaml . | nindent 4}}
  {{- end }}
{{- end }}