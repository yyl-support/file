{{- with .Values.istio }}
{{- if .enabled }}
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ include "community-sig-monitor.fullname" $ }}-virtualservice
spec:
  gateways:
  - {{ .gateway }}
  hosts:
  - {{ .host }}
  http:
    {{- toYaml .vs.http | nindent 4 }}


{{- with .dr }}
---
apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: {{ include "community-sig-monitor.fullname" $ }}-destinationrule
spec:
  host: {{ .serviceName }}
  trafficPolicy:
    portLevelSettings:
      - port:
          number: {{ .portNumber }}
        tls:
          mode: {{ $.Values.istio.tlsMode }}
{{- end }}


---
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: {{ include "community-sig-monitor.fullname" $ }}-peerauthentication
spec:
  mtls:
    mode: {{ $.Values.istio.mtlsMode }}
  selector:
    matchLabels:
      {{- include "community-sig-monitor.selectorLabels" $ | nindent 6 }}
{{- end }}
{{- end }}