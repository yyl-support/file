{{- range $name, $secret := .Values.secretDefinitions }}
{{- if $secret.enabled }}
---
apiVersion: secrets-manager.tuenti.io/v1alpha1
kind: SecretDefinition
metadata:
  name: {{ $name }}
spec:
  keysMap:
    {{- range $key, $value := $secret.keysMap }}
    {{ $key }}:
      key: {{ $value.key }}
      path: {{ $value.path }}
    {{- end }}
  name: {{ $name }}
{{- end }}
{{- end }}
