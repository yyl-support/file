#chartVersion: 1.0.9
namespace:
  enabled: false
  name: ascend
serviceAccount:
  create: false
  name: ascend
strategy:
  rollingUpdate:
    maxSurge: 2
    maxUnavailable: 2
  type: RollingUpdate
image:
  center:
    name: meeting-center
    repository: swr.cn-southwest-2.myhuaweicloud.com/ascend_infra/meeting/meeting-center
    tag: "main-f4c504"
    imagePullPolicy: IfNotPresent
  platform:
    name: meeting-platform
    repository: swr.cn-southwest-2.myhuaweicloud.com/ascend_infra/meeting/meeting-platform
    tag: "main-973898"
    imagePullPolicy: IfNotPresent
initContainer:
  image:
    repository: swr.cn-southwest-2.myhuaweicloud.com/ascend_infra/openeuler
    tag: 22.03-lts-sp3
podAnnotations:
  vault.hashicorp.com/agent-init-first: "true"
  vault.hashicorp.com/agent-inject: "true"
  vault.hashicorp.com/agent-pre-populate-only: "true"
  vault.hashicorp.com/agent-run-as-group: "1000"
  vault.hashicorp.com/agent-run-as-user: "1000"
  vault.hashicorp.com/agent-service-account-token-volume-name: token-vol
  vault.hashicorp.com/role: ascend
  vault.hashicorp.com/tls-skip-verify: "true"
  vault.hashicorp.com/log-level: "warn"
  vault.hashicorp.com/agent-inject-perms-ca.pem: "0400"
  vault.hashicorp.com/agent-inject-perms-server.crt: "0400"
  vault.hashicorp.com/agent-inject-perms-server.key: "0400"
  vault.hashicorp.com/agent-inject-secret-ca.pem: internal/data/ascend/ascend-server
  vault.hashicorp.com/agent-inject-secret-server.crt: internal/data/ascend/domain-tls
  vault.hashicorp.com/agent-inject-secret-server.key: internal/data/ascend/domain-tls
  vault.hashicorp.com/agent-inject-template-ca.pem: |
    {{- with secret "internal/data/ascend/ascend-server" -}}
    {{ .Data.data.meetingMysqlCA }}
    {{- end }}
  vault.hashicorp.com/agent-inject-template-server.crt: |
    {{- with secret "internal/data/ascend/domain-tls" -}}
    {{ .Data.data.ServerCrt }}
    {{- end }}
  vault.hashicorp.com/agent-inject-template-server.key: |
    {{- with secret "internal/data/ascend/domain-tls" -}}
    {{ .Data.data.ServerKey }}
    {{- end }}
podSecurityContext:
  seccompProfile:
    type: Localhost
    localhostProfile: infra-seccomp.json
securityContext:
  allowPrivilegeEscalation: false
  capabilities:
    drop:
      - ALL
  runAsUser: 1000
ingress:
  enabled: false
  host: ""
  port:
    number: 80
# 后端使用ingress路径转发时启用ingressNew
ingressNew:
  enabled: true
  name: cann-meeting-center-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /api/v1/meeting$1
  host: meeting.osinfra.cn
  path: /cann-meeting(.*)
  pathType: ImplementationSpecific
  secretName: osinfra-cn-tls
istio:
  enabled: false
  nslabel: {}
  # istio-injection: enabled
  gateway: "istio-system/istio-gateway-https"
  host: ""
  # vs:
  #   http:
  #     - match:
  #       - uri:
  #           prefix: /api-platform/
  #       rewrite:
  #         uri: /
  #       route:
  #       - destination:
  #           host: meeting-platform.meetingserver.svc.cluster.local
  #           port:
  #             number: 8080
  #     - match:
  #       - uri:
  #           prefix: /
  #       rewrite:
  #         uri: /
  #       route:
  #       - destination:
  #           host: meeting-center.meetingserver.svc.cluster.local
  #           port:
  #             number: 8080
  # dr:
  # - name: meeting-center
  #   serviceName: meeting-center
  #   portNumber: 8080
  #   tlsMode: SIMPLE
  # - name: meeting-platform
  #   serviceName: meeting-platform
  #   portNumber: 8080
  #   tlsMode: SIMPLE
  # pa:
  # - name: meeting-center
  #   mtlsMode: DISABLE
  #   label: 
  #     app: meeting-center
  # - name: meeting-platform
  #   mtlsMode: DISABLE
  #   label: 
  #     app: meeting-platform
meetingCenter:
  enabled: true
  name: cann-meeting-center
  replicas: 3
  podLabels:
    app: cann-meeting-center
  resources:
    limits:
      cpu: "1"
      memory: 1Gi
    requests:
      cpu: "1"
      memory: 1Gi
  service:
    type: ClusterIP
    annotations: {}
    ports:
      - name: http-port
        port: 8080
        protocol: TCP
        targetPort: 8080
  podAnnotations:
    vault.hashicorp.com/agent-inject-perms-config: "0400"
    vault.hashicorp.com/agent-inject-perms-secrets.yaml: "0400"
    vault.hashicorp.com/agent-inject-secret-config: internal/data/ascend/ascend-server
    vault.hashicorp.com/agent-inject-secret-secrets.yaml: internal/data/ascend/ascend-server
    vault.hashicorp.com/agent-inject-template-config: |
      {{- with secret "internal/data/ascend/ascend-server" -}}
      {{ .Data.data.meetingCenterConfig }}
      {{- end }}
    vault.hashicorp.com/agent-inject-template-secrets.yaml: |
      {{- with secret "internal/data/ascend/ascend-server" -}}
      {{ .Data.data.meetingCenterSecrets }}
      {{- end }}
meetingPlatform:
  enabled: true
  name: cann-meeting-platform
  replicas: 3
  podLabels:
    app: cann-meeting-platform
  resources:
    limits:
      cpu: "1"
      memory: 1Gi
    requests:
      cpu: "1"
      memory: 1000Mi
  service:
    ports:
      - name: http-port
        port: 8080
        protocol: TCP
        targetPort: 8080
    type: ClusterIP
  podAnnotations:
    vault.hashicorp.com/agent-inject-perms-config: "0400"
    vault.hashicorp.com/agent-inject-perms-kafka.crt: "0400"
    vault.hashicorp.com/agent-inject-perms-secrets.yaml: "0400"
    vault.hashicorp.com/agent-inject-secret-config: internal/data/ascend/ascend-server
    vault.hashicorp.com/agent-inject-secret-kafka.crt: internal/data/ascend/ascend-server
    vault.hashicorp.com/agent-inject-secret-secrets.yaml: internal/data/ascend/ascend-server
    vault.hashicorp.com/agent-inject-template-config: |
      {{- with secret "internal/data/ascend/ascend-server" -}}
      {{ .Data.data.meetingPlatformconfig }}
      {{- end }}
    vault.hashicorp.com/agent-inject-template-kafka.crt: |
      {{- with secret "internal/data/ascend/ascend-server" -}}
      {{ .Data.data.meetingPlatformKafkacrt }}
      {{- end }}
    vault.hashicorp.com/agent-inject-template-secrets.yaml: |
      {{- with secret "internal/data/ascend/ascend-server" -}}
      {{ .Data.data.meetingPlatformSecrets }}
      {{- end }}
secret:
  enabled: false
  # name: ascend-domain-tls
  # specKeysMap:
  #   ca.crt:
  #     path: secrets/data/ascend/meeting-osinfra-cn-tls
  #     key: server.crt
  #   tls.crt:
  #     path: secrets/data/ascend/meeting-osinfra-cn-tls
  #     key: server.crt
  #   tls.key:
  #     path: secrets/data/ascend/meeting-osinfra-cn-tls
  #     key: server.key
cronjobCommon:
  securityContext:
    allowPrivilegeEscalation: false
    capabilities:
      drop:
        - ALL
    readOnlyRootFilesystem: false
    runAsUser: 1000
  env:
    - name: TZ
      value: Asia/Shanghai
    - name: CONFIG_PATH
      value: /vault/secrets/config
    - name: VAULT_PATH
      value: /vault/secrets/secrets.yaml
    - name: MYSQL_TLS_PEM_PATH
      value: /vault/secrets/ca.pem
    - name: TLS_CRT_PATH
      value: /vault/secrets/server.crt
    - name: TLS_KEY_PATH
      value: /vault/secrets/server.key
cronjobs:
  - enabled: true
    name: cann-handle-meeting
    labels:
      app: cann-handle-meeting
    concurrencyPolicy: Forbid
    schedule: "*/15 * * * *"
    suspend: false
    restartPolicy: OnFailure
    podAnnotations:
      vault.hashicorp.com/agent-inject-perms-config: "0400"
      vault.hashicorp.com/agent-inject-perms-secrets.yaml: "0400"
      vault.hashicorp.com/agent-inject-secret-config: internal/data/ascend/ascend-server
      vault.hashicorp.com/agent-inject-secret-secrets.yaml: internal/data/ascend/ascend-server
      vault.hashicorp.com/agent-inject-template-config: |
        {{- with secret "internal/data/ascend/ascend-server" -}}
        {{ .Data.data.meetingPlatformconfig }}
        {{- end }}
      vault.hashicorp.com/agent-inject-template-secrets.yaml: |
        {{- with secret "internal/data/ascend/ascend-server" -}}
        {{ .Data.data.meetingPlatformSecrets }}
        {{- end }}
    command:
      - /bin/sh
      - -c
      - |
        python3 manage.py handle_meeting
    image:
      repository: swr.cn-southwest-2.myhuaweicloud.com/ascend_infra/meeting/meeting-platform
      tag: "main-973898"
      imagePullPolicy: IfNotPresent
    resources:
      limits:
        cpu: 500m
        memory: 500Mi
      requests:
        cpu: 500m
        memory: 500Mi
  - enabled: true
    name: cann-handle-recording
    labels:
      app: cann-handle-recording
    concurrencyPolicy: Forbid
    schedule: "30 * * * *"
    suspend: false
    restartPolicy: OnFailure
    podAnnotations:
      vault.hashicorp.com/agent-inject-perms-config: "0400"
      vault.hashicorp.com/agent-inject-perms-secrets.yaml: "0400"
      vault.hashicorp.com/agent-inject-secret-config: internal/data/ascend/ascend-server
      vault.hashicorp.com/agent-inject-secret-secrets.yaml: internal/data/ascend/ascend-server
      vault.hashicorp.com/agent-inject-template-config: |
        {{- with secret "internal/data/ascend/ascend-server" -}}
        {{ .Data.data.meetingPlatformconfig }}
        {{- end }}
      vault.hashicorp.com/agent-inject-template-secrets.yaml: |
        {{- with secret "internal/data/ascend/ascend-server" -}}
        {{ .Data.data.meetingPlatformSecrets }}
        {{- end }}
    command:
      - /bin/sh
      - -c
      - |
        python3 manage.py handle_recordings
    image:
      repository: swr.cn-southwest-2.myhuaweicloud.com/ascend_infra/meeting/meeting-platform
      tag: "main-973898"
      imagePullPolicy: IfNotPresent
    resources:
      limits:
        cpu: 6
        memory: 1Gi
      requests:
        cpu: 6
        memory: 1Gi
  - enabled: true
    name: cann-handle-meeting-status
    labels:
      app: cann-handle-meeting-status
    concurrencyPolicy: Forbid
    schedule: "*/5 * * * *"
    suspend: false
    restartPolicy: OnFailure
    podAnnotations:
      vault.hashicorp.com/agent-inject-perms-config: "0400"
      vault.hashicorp.com/agent-inject-perms-secrets.yaml: "0400"
      vault.hashicorp.com/agent-inject-secret-config: internal/data/ascend/ascend-server
      vault.hashicorp.com/agent-inject-secret-secrets.yaml: internal/data/ascend/ascend-server
      vault.hashicorp.com/agent-inject-template-config: |
        {{- with secret "internal/data/ascend/ascend-server" -}}
        {{ .Data.data.meetingPlatformconfig }}
        {{- end }}
      vault.hashicorp.com/agent-inject-template-secrets.yaml: |
        {{- with secret "internal/data/ascend/ascend-server" -}}
        {{ .Data.data.meetingPlatformSecrets }}
        {{- end }}
    command:
      - /bin/sh
      - -c
      - |
        python3 manage.py handle_meeting_status
    image:
      repository: swr.cn-southwest-2.myhuaweicloud.com/ascend_infra/meeting/meeting-platform
      tag: "main-973898"
      imagePullPolicy: IfNotPresent
    resources:
      limits:
        cpu: 500m
        memory: 500Mi
      requests:
        cpu: 500m
        memory: 500Mi
