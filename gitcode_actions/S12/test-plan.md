# S12 测试计划

## 1. 当前状态

**BLOCKED：缺少 S12 与 Excel 用例 ID 的权威映射。**

当前本地目录此前没有 `S12/`，也没有 S12 的测试计划、测试记录或 YAML。`gitcode-pipeline-test-cases.xlsx` 包含用例内容和预期结果，但未包含负责人、S 编号或模块分配字段，因此不能仅根据 Excel 推断哪些 TC 属于 S12 或属于 yulin。

## 2. 后续处理规则

1. 提供 S12 的 TC 编号清单或权威分配来源后，先将每条 Excel 预期写入本计划的评审表。
2. 每个 TC 先明确前置条件、唯一断言、证据路径、清理动作和 `TC-<编号>-<name>.yml` 文件名。
3. 完成评审后才创建 YAML 和执行测试；不得用其他 S 模块的用例替代 S12。
4. 所有 S12 文件和证据先保留在本地，统一提交前再进行完整复核。

## 3. 待补充信息

需要以下任一资料：S12 TC-ID 列表、包含 S12 分配的原始表格列，或项目负责人确认的用例范围。
