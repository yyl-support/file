# S13 Push 与工作流能力验证测试记录

## 1. 当前状态

**PENDING（尚未开始执行）**。

当前没有已完成的 S13 Workflow Run、Job API 或脱敏日志证据，因此所有用例仍处于待执行状态。不得基于计划内容、文件名或其他模块的结果填写 PASS、FAIL、PARTIAL、BLOCKED 或 SKIP。

## 2. 已分配范围

`TC-035`、`TC-036`、`TC-194`、`TC-195`、`TC-220`、`TC-273`、`TC-304`、`TC-354`、`TC-355`、`TC-387`、`TC-388`、`TC-389`、`TC-390`、`TC-391`、`TC-535`。

## 3. 执行前待确认项

| 前置条件 | 影响用例 | 确认后才可执行 |
| --- | --- | --- |
| 组织级与项目级同名变量/Secret 均可配置 | TC-194、TC-195、TC-535 | 是 |
| 受控测试分支和 push 权限 | TC-035、TC-036、TC-220、TC-304、TC-354、TC-355、TC-387 | 是 |
| 容器 Runner、Docker 和镜像仓库权限 | TC-273、TC-390 | 是 |
| 测试 Merge Request | TC-388 | 是 |
| 测试 Tag/Release 权限 | TC-389 | 是 |
| 可用 Scheduler 与完整观察窗口 | TC-391 | 是 |
| 可读取且可脱敏的日志 | TC-036、TC-354 及所有敏感变量用例 | 是 |

## 4. 执行记录模板

每次完成一个 TC 后追加一行，未执行用例保持空白状态：

| TC | Workflow 文件 | 触发提交/Run ID | Job ID | 实际结果 | 状态 | 本地证据目录 |
| --- | --- | --- | --- | --- | --- |
| TC-000 | `TC-000-example.yml` | - | - | 未执行 | PENDING | `evidence/TC-000/` |

## 5. 记录要求

1. 先写入本地 `run.json`、`job.json` 和 `notes.md`，再更新此记录的状态。
2. 任何 Token、Secret、完整日志或凭据均不得以明文写入本文件。
3. 若平台日志接口不可用，必须记录接口状态和替代证据，不能把敏感输出类用例直接标记 PASS。
4. 触发测试结束后删除临时 Workflow 触发条件或测试分支，保留清理结果。
