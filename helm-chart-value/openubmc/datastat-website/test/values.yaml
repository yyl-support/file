# 全局配置
global:
  namespace: openubmc
  appName: datastat-website
# 副本配置

replicaCount: 1
revisionHistoryLimitCount: 4
# 镜像配置

image:
  repository: swr.cn-north-4.myhuaweicloud.com/opensourceway/datastat/openubmc-test
  tag: "master-temp-82753a"
  pullPolicy: IfNotPresent
  pullSecret: huawei-swr-image-pull-secret
podLabels:
  app: datastat-website
podAnnotations:
  vault.hashicorp.com/agent-inject: "true"
  vault.hashicorp.com/agent-inject-perms-server.crt: "0400"
  vault.hashicorp.com/agent-inject-perms-server.key: "0400"
  vault.hashicorp.com/agent-inject-perms-abc.txt: "0400"
  vault.hashicorp.com/agent-inject-perms-dhparam.pem: "0400"
  vault.hashicorp.com/agent-run-as-user: "1000"
  vault.hashicorp.com/agent-run-as-group: "1000"
  vault.hashicorp.com/secret-volume-path: "/etc/nginx/cert/"
  vault.hashicorp.com/agent-inject-secret-server.crt: "internal/data/infra-test/openubmc-domain-tls"
  vault.hashicorp.com/agent-inject-template-server.crt: |
    {{- with secret "internal/data/infra-test/openubmc-domain-tls" -}}
    {{ .Data.data.ServerCrt }}
    {{- end }}
  vault.hashicorp.com/agent-inject-secret-server.key: "internal/data/infra-test/openubmc-domain-tls"
  vault.hashicorp.com/agent-inject-template-server.key: |
    {{- with secret "internal/data/infra-test/openubmc-domain-tls" -}}
    {{ .Data.data.ServerKey }}
    {{- end }}
  vault.hashicorp.com/agent-inject-secret-abc.txt: "internal/data/infra-test/openubmc-domain-tls"
  vault.hashicorp.com/agent-inject-template-abc.txt: |
    {{- with secret "internal/data/infra-test/openubmc-domain-tls" -}}
    {{ .Data.data.certificatePassword }}
    {{- end }}
  vault.hashicorp.com/agent-inject-secret-dhparam.pem: "internal/data/infra-test/openubmc-domain-tls"
  vault.hashicorp.com/agent-inject-template-dhparam.pem: |
    {{- with secret "internal/data/infra-test/openubmc-domain-tls" -}}
    {{ .Data.data.dhparamPem }}
    {{- end }}
  vault.hashicorp.com/role: openubmc-website
  vault.hashicorp.com/agent-service-account-token-volume-name: "token-vol"
  vault.hashicorp.com/tls-skip-verify: "true"
  vault.hashicorp.com/agent-pre-populate-only: "true"
  vault.hashicorp.com/agent-init-first: "true"
  vault.hashicorp.com/agent-inject-command-abc.txt: |
    if [ $(netstat -anlp | grep 8080| wc -l) -ge 1 ]; then rm -rf /etc/nginx/cert/*; else echo "service not running skip"; fi
  vault.hashicorp.com/template-static-secret-render-interval: 5s
  vault.hashicorp.com/log-level: "warn"
env:
  - name: DET_URL
    value: https://datastat.openubmc.test.osinfra.cn/zh/overview
serviceAccount:
  create: false
  name: openubmc
  automount: false
# 资源限制

resources:
  requests:
    cpu: 1000m
    memory: 1000Mi
  limits:
    cpu: 1000m
    memory: 1000Mi
# Ingress配置

ingress:
  enabled: false
  className: nginx
  annotations: {}
  host: ""
# Service配置

service:
  portName: tls-01
  type: ClusterIP
  port: 8080
  targetPort: 8080
secret:
  enable: false
  name: ""
  path: ""
  ca_crt_key: tls.cert
  tls_crt_key: tls.cert
  tls_key_key: tls.key
istio:
  enabled: true
  nslabel:
    istio-injection: enabled
  gateway: "istio-system/istio-gateway-https-02"
  virtualService:
    host: datastat.openubmc.test.osinfra.cn
    routes:
      - destinations:
          - host: datastat-website-service.openubmc.svc.cluster.local
        match:
          - uri:
              prefix: /
  tls:
    mode: SIMPLE
  mtls:
    mode: DISABLE
