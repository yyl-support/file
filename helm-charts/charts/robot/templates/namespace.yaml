{{- with .Values.namespace }}
{{- if .enabled }}
apiVersion: v1
kind: Namespace
metadata:
  {{- if $.Values.istio.nslabel }}
  labels:
    {{- toYaml $.Values.istio.nslabel | nindent 4 }}
  {{- end }}
  name: {{ .name }}
{{- end }}
{{- end }}
