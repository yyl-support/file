{{- if .Values.namespace.enabled }}
apiVersion: v1
kind: Namespace
metadata:
  {{- if .Values.istio.enable }}
  labels:
    {{- toYaml .Values.istio.nslabel | nindent 4 }}
  {{- end }}
    name: {{ .Values.namespace.name }}
  name: {{ .Values.namespace.name }}
{{- end }}
