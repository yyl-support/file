apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.statement.name }}-service
  labels:
    {{- with .Values.statement.podLabels }}
    {{- toYaml . | nindent 4}}
    {{- end }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.statement.service.port }}
      targetPort: {{ .Values.statement.service.targetPort }}
      protocol: TCP
      name: http-port
  {{- with .Values.statement.podLabels }}
  selector:
    {{- toYaml . | nindent 4}}
  {{- end }}