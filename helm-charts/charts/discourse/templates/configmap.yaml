{{- range .Values.configmaps }}
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .name }}
  namespace: {{ $.Values.namespace.name }}
data:
  {{ .keyName }}: |
{{ .content | nindent 4 }}
{{- end }}