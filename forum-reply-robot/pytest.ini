[pytest]
# 禁用 CI 缓存环境中全局预装的 pytest-asyncio 插件自动加载。
# 该环境 pytest 被钉为 7.4.4，而 pytest-asyncio>=0.24 会执行 `from pytest import FixtureDef`
# （FixtureDef 在 pytest 8.x 才提升为公共符号），导致 pytest 启动即崩溃、coverage.xml 无法生成。
# 本项目不使用任何异步测试，直接屏蔽该插件即可，无需改动共享 CI 脚本。
addopts = -p no:asyncio
